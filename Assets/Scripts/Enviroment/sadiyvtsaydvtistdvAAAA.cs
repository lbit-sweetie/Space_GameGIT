using UnityEngine;

public class sadiyvtsaydvtistdvAAAA : MonoBehaviour
{
    private void Start()
    {
        var HAshdhashd = Camera.main.orthographicSize * 2f;
        var Wakkasgfkahsf = HAshdhashd * Screen.width / Screen.height * 1.5f;

        transform.localScale = new Vector3(HAshdhashd, HAshdhashd, 0);
    }
}
